This readme will be for keeping helpful commands for GPU and CUDA stuff.

  nvidia-smi
    A CLI tool to get information about GPUs.

    Run nvidia-smi without any arguments to get basic information about the GPU
    on the device.

  nvcc
    The CUDA compiler driver.

    Run "nvcc -v" to see what version of CUDA is installed.
